menuconfig NOTIFY_LED
    bool "信息通知LED"
    help
        启用基于信息通知LED

if NOTIFY_LED


module = NOTIFY_LED
module-str = NotifyLED
source "subsys/logging/Kconfig.template.log_config"

endif # NOTIFY_LED