sample:
  description: One-Framework application
  name: one-framework
common:
  build_only: true
  integration_platforms:
    - dji_board_c
tests:
  app.infantry:
    extra_args: DTC_OVERLAY_FILE=boards/cone/infantry.overlay
    extra_overlay_confs:
      - debug.conf
